import SelectedBreed from './SelectedBreed'
import SelectedBreedContent from './SelectedBreedContent'
import { SelectedBreedProps, SelectedBreedContentProps } from './types'
import { boxStyle } from './utils'

export { SelectedBreed, SelectedBreedContent, boxStyle }
export type { SelectedBreedProps, SelectedBreedContentProps }
